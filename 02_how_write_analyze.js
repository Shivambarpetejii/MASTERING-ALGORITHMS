// 🔹 How to Write an Algorithm
        // When writing an algorithm, keep it clear, structured, and step-by-step.
            //  * General Rules
                //  * Start with Input and Output
                //  * What goes in (input)?
                //  * What should come out (output)?
                //  * Write finite steps (end at some point).
                //  * Use simple language or pseudocode.
                //  * Each step should be unambiguous (clear meaning).

    // Example: Algorithm to Add Two Numbers
            // Input :- Two Numbers A and B.
            // Output :- Their sum SUM

            //Steps of algorithm:-
                    // 1. Start
                    // 2. input two numbers A and B-------------(1)[1]
                    // 3. create store SUM----------------------(1)[1]
                    // 4. sum = A+B this is calculation of maths 
                    // 5. return the answer || print the answer
                    // 6. end

            //program baseed on algo Second Algo :- 

                     let A = 10;
                     let B = 20;
                     let SUM = 0;
                     SUM = A+B;
                     console.log("this is sum of two number:-",SUM );
// 🔹 How to Analyze an Algorithm
            // Analysis means understanding performance:

                    // *  How fast it runs
                    // *  How much memory it uses


                    //time :- O(1)
                    //space :-O(1)


//=============================================================================================================

//Sum of array write the algorithum
             
        // Algorithm:-
                    // 1. Start.
                    // 2. Take input Arr(array), N(count)------------------------------(time = 1)
                    // 3. Create the variable for store the Sum-------------------------(time =1)
                    // 4. triversh the array from 0 To n-1--------------------------------(time = condication check n+1)
                            // 5. inside the trivershing Sum all aliment one by one----------(time = n)
                    // 6. return answer || print answer;------------------------------------(time = 1)


                    //time = O(n)
                    //space = O(1)

//=============================================================================================================
